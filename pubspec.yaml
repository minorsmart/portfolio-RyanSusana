name: portfolio_minor
description: Ryan's portfolio

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flare_flutter: ^2.0.1
  flutter_staggered_animations: ^0.1.2
  flutter_parallax: ^0.1.2
  fluro: 1.6.3
  http: ^0.12.0+4
  uuid: 2.0.4
  uri: ^0.11.3+1
  url_launcher: ^5.4.2
  flutter_html: ^0.11.1
  provider: ^4.0.4
  json_annotation: 3.0.1
  google_fonts: ^0.3.9
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2

dev_dependencies:
  build_runner: 1.8.0
  json_serializable: ^3.2.5
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/header.jpg
